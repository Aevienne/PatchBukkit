# §16 Fork Audit — `diag/playasia-jvm-core-dump` vs upstream `Pumpkin-MC/PatchBukkit` master

> Executed 2026-09-05 (GitHub compare API). Status: AUDITED — merge NOT yet performed.
> Purpose: classify every divergence and decide what (if anything) to pull from upstream.

## 1. Divergence summary

| Comparison | Status | Ahead | Behind |
|---|---|---|---|
| `Pumpkin-MC/PatchBukkit` master → `Aevienne/PatchBukkit` `diag/playasia-jvm-core-dump` | diverged | **105** (ours only) | **1** (upstream only) |
| `Aevienne/PatchBukkit` master → diag | ahead | 69 | 0 |

- **All 105 commits on diag are ours** (fork-master work + Sep 3–5 diag work: registry fix `c048461`, command-system port `9030bc0`, workflow fixes `4761823`/`c6869cc`, pumpkin pins `526a4cc`/`1319806`, wrapper/egg work, deploy fixes). Nothing upstream has that we need is missing except the single commit below.
- **Fork `master` is fully contained in diag** (behind 0) — diag is a strict superset. This means §16's "never synced" concern is about *upstream advances*, not fork-internal drift.

## 2. The one upstream-only commit: `9d23556` "update to latest pumpkin" (2026-09-05 06:15 UTC)

This is a dependency/payload bump against the newest upstream Pumpkin API. It is the **only** thing we are behind on.

### 2.1 Files touched

| File | Δ (+/−) | Verdict | Notes |
|---|---|---|---|
| `rust/Cargo.lock` | +513 / −419 | **CONFLICT → TAKE (gated)** | Our lock pins pumpkin git deps to `aaadb792` (per `1319806`, Server.random_sequences layout drift) and earlier `99989ba`; upstream's lock is a different rev lineage. Merge will conflict. Resolution: adopt upstream's new rev, re-pin `Cargo.toml` to the *same* rev, regenerate the lock. |
| `rust/src/commands/mod.rs` | +59 / −92 | **CONFLICT → TAKE (gated)** | Same file we ported to the new command system in `9030bc0` (CommandContext/ExecutorResult, GreedyPhrase, SuggestionProvider, CommandSyntaxError, TargetSelector → direct world scan, teleport via EntityBase, `&TextComponent` header/footer). Expect textual conflicts; re-apply our PlayAsia-specific deltas onto upstream's version. |
| `rust/src/java/jvm/commands.rs` | +2 / −2 | TAKE | Arity/signature alignment with new API; trivial. |
| `rust/src/java/jvm/worker.rs` | +2 / −2 | TAKE | Same class of change. |
| `rust/src/java/native_callbacks/command.rs` | +9 / −2 | TAKE | Command-result API surface; verify against our command port. |
| `rust/src/java/native_callbacks/entity.rs` | +1 / −1 | TAKE | Trivial. |
| `rust/src/java/native_callbacks/location.rs` | +22 / −25 | TAKE | Rotation/value API changes; verify against `9030bc0`'s teleport. |
| `rust/src/java/native_callbacks/sound.rs` | +9 / −11 | TAKE | Sound API alignment. |
| `rust/src/java/plugin/command_manager.rs` | +3 / −4 | TAKE | Trivial. |

Nothing in the batch touches our registry/FFI/worker/JVM-hardening surface (`java/`, `proto/`, bootstrap, `jvm/worker.rs` beyond 2 lines) — the struct-layout risk from the Sep-4 exit-139 saga is therefore confined to the pumpkin dep bump itself (see §3).

### 2.2 What TAKE (gated) means

`9d23556` is NOT safe to merge in place today, for three reasons:

1. **Bridge-pin drift gate (§15 of AI_WORKFLOW_GUIDE.md).** The bump moves the pumpkin rev our Rust side compiles against. The deployed binary layout must match the bridge's pin exactly (Sep-4 lesson: 4-field `LoggingConfig` shift → SIGSEGV). **Procedure: rebuild the bridge against the new rev FIRST (pin bump + `build-linux` + deploy bridge `.so`/`.jar` to Pumpkin DEV), then deploy the new pumpkin binaries.**
2. **Command-API conflicts with our own port** (`commands/mod.rs` + `jvm/commands.rs` + `native_callbacks/command.rs`) — must be resolved deliberately against `9030bc0`, not via `--ours`/`--theirs`.
3. **Lockfile conflict** is guaranteed (different pin lineage).

### 2.3 Recommended merge plan (not yet executed)

1. Scratch branch `merge/upstream-20260905` off current diag.
2. `git merge Pumpkin-MC/master` (or cherry-pick `9d23556`).
3. Resolve `Cargo.lock`: adopt upstream rev; bump `rust/Cargo.toml` pin → same rev; `cargo update` + lock refresh (mirrors `526a4cc`/`1319806` pattern).
4. Resolve `commands/mod.rs` conflicts; re-apply: direct `server.worlds` TargetSelector scan, `EntityBase` teleport, `&TextComponent` header/footer, `StringArgumentType::GreedyPhrase`, `CommandSyntaxError` mapping.
5. `./gradlew :patchbukkit:compileJava` + local `cargo check` (needs `java/patchbukkit/build/libs/patchbukkit.jar` — run `:patchbukkit:jar` first).
6. Push branch → `build-linux.yml` green.
7. **§15 order:** rebuild + deploy bridge to Pumpkin DEV (pin bump first), smoke `timeout 90` (exit 124 = healthy), then dispatch `deploy-pumpkin.yml` (target `pumpkin-dev`) for the new pumpkin binary; conformance spot-check.
8. Only then merge diag → master (owner approval checkpoint).
9. **Do NOT open PRs / touch upstream** (hard rule).

## 3. Timeline alignment note

`9d23556` was pushed 06:15 UTC; our own pin bump `131980698` (to `aaadb792`) landed 06:39 UTC — 24 minutes later, independently, against the *pre-bump* upstream. Layout compatibility of our current pin with the Sep-4-era deployed binary remains the constraint until this audit's merge runs (§2.3).

## 4. Status

- [ ] Merge `9d23556` on scratch branch
- [ ] Bridge pin bump + rebuild + deploy to Pumpkin DEV
- [ ] Deploy new pumpkin binary + smoke test (exit 124)
- [ ] diag → master merge (owner approval)